# Setup

## Dependencies
- Python 3.10+
- `curses` (included in Python stdlib on macOS/Linux)

## Install
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run
```bash
python src/editor.py [filename]
```
